const array = [
    {"id": "1"},
    {"id": "1"},
    {"id": "2"}
]

function groupFunction(array) {
    // const object = new Object();
    // array.forEach((obj) => {
    //     let ID = obj.id;
    //     object.ID = 
    //     // object.id.push(obj.id)
    // })

    // return console.log(object)
    const object = new Object();
    array.for
    for(let i = 0; i < array.length; i++) {
        let ID = array[i].id;
        object.ID = array[i].id
    }
    return console.log(object)
}
groupFunction(array)